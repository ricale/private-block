<%= react_component('Root', {
  session: {
    authenticityToken: form_authenticity_token,
    valid: !current_user.nil?,
    user: current_user,
    ogImageUrl: image_url('ricalest.png')
  },
}.merge(props)) %>
